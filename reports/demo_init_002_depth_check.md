# demo_init_002 数据检查

检查对象：`datas/demo_init_002`，2026-09-22 11:21:53 开始录制的 episode 0。
本报告对应 1 个 episode、967 帧的版本。检查过程没有改写原始数据。

## 结论

episode 0 已完整保存，标签为 `success`，按 30 FPS 计算约 32.23 秒。
这次异常发生在空的 episode 1 保存时，没有破坏 episode 0。
LeRobotDataset 可以加载该数据集，并读取首帧、中间帧和最后一帧的两路 RGB。

## 完整性

| 检查项 | head 第三视角 | wrist 腕部 |
| --- | --- | --- |
| 相机序列号 | CP9KB5300030 | CP9KB5300059 |
| 实际解码 RGB 帧数 | 967 | 967 |
| 深度 PNG 数量 | 967 | 967 |
| 深度尺寸、类型 | 480×640，uint16 | 480×640，uint16 |
| 原始深度单位 | 1 mm | 1 mm |
| PNG 读取失败 | 0 | 0 |
| 整张深度为零 | 0 | 0 |
| 连续完全相同的深度图 | 0 | 0 |
| RGB 损坏帧 | 0 | 0 |
| RGB 与深度设备时间戳差，最大 | 1 ms | 2 ms |

逐张读取了全部 1934 张深度 PNG，完整解码了两路视频。
Parquet 状态/动作行数、episode 元数据长度、深度清单长度均为 967。
全局帧号和 episode 内帧号为连续的 0–966；清单时间戳与 Parquet 时间戳一致。
状态、动作和记录的策略动作均无 NaN/Inf。
深度清单中的 RGB 字段、相机序列号、图像尺寸与标定文件一致。

## 深度质量与使用限制

| 指标 | head | wrist |
| --- | --- | --- |
| 非零深度像素比例，平均 | 90.20% | 63.03% |
| 非零深度像素比例，逐帧最低 | 87.76% | 45.86% |
| 非零像素抽样距离 P1 / P50 / P99 | 252 / 661 / 2601 mm | 168 / 587 / 4377 mm |
| 原始值为 65535 的像素总数 | 64 | 342032 |
| 设备时间戳间隔最大值 | 67 ms | 66 ms |
| 超过 50 ms 的采样间隔次数 | 1 | 1 |

腕部深度平均约 37% 像素为零，属于缺测区域，不应解释为物体距离 0 mm。
腕部还有约 0.115% 的像素为 65535，应作为饱和/超量程候选值过滤，结合实际工作距离设置范围。
非零比例不等同于测距精度；本次检查没有用标定目标验证绝对距离误差。

两路各有一次约两个帧周期的时间戳间隔，可视为采样跳过了约一帧；保存文件没有缺号。
双相机主机到帧时刻之差平均约 9.97 ms、最大约 27.72 ms，不属于双相机硬件同步。

深度为原生传感器坐标，`aligned_to_color=false`，不能直接与 RGB 逐像素叠加。
RGB/深度内参保存在 `depth/episode_000000/cameras.json`。

## 异常原因与修复

保存/编码期间的额外 `s` 按键设置了 `exit_early`，旧流程在下一段只清除
`episode_outcome`，未清除 `exit_early`，导致 episode 1 在采集第一帧前退出。
随后保存空缓冲区触发 `You must add one or several frames...`。

已在录制段开始时清除残留退出事件，保持停止录制请求有效，并在保存前跳过空段。
相关 5 项回归测试通过，涵盖编码期间按 `s`、空段、停止请求和零时长配置。

该数据集可以保留。继续采集可在之前成功启动的相同配置上增加 `--resume=true`，
保持 repo_id、root、相机名称和数据格式一致；新增数据从 episode 1 开始编号。
